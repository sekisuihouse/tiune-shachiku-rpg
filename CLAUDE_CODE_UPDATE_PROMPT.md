# Claude Code 用アップデートプロンプト

以下を守って、このリポジトリのゲームを改善してください。

1. まず `CLAUDE_CODE_HANDOVER.md` と `README.md` を読み、`index.html` を確認する。
2. 本ゲームはビルド不要のCanvasゲームである。既存のアセット名とGitHub Pages互換の相対パスを維持する。
3. `node --check <(sed -n '/<script>/,/<\/script>/p' index.html | sed '1d;$d')` を、変更後に必ず実行する。
4. プレイヤーの戦闘当たり判定は `playerHitbox()`、足場判定は `solidMove()` を使う。見た目に合わせて安易に物理サイズを広げない。
5. エナドリを取得または使用する新規アクションでは、必ず `playEnergySound()` を呼ぶ。
6. 状態遷移を追加・変更する場合、`update()` と `draw()` を同期させ、リトライ時に初期化が必要な変数を `reset()` へ追加する。
7. 現在の要件を壊さず、読みやすい関数分割を優先する。外部ライブラリは導入しない。
8. 実装後、変更ファイル・挙動・検証コマンド結果を簡潔に報告する。

最初の作業として、上記の引き継ぎ書類と実装の差分がないか確認し、差分があれば安全に修正してください。
