# Claude Code 引き継ぎ書類 — TIUNE 社畜RPG

## 目的

このリポジトリは、ブラウザだけで動作するCanvas製の横スクロールアクションゲームです。エントリーポイントは `index.html` のみで、ビルド工程・外部依存・パッケージ管理はありません。

## 実行と確認

- `index.html` をブラウザで開く。
- 構文確認: `node --check <(sed -n '/<script>/,/<\/script>/p' index.html | sed '1d;$d')`
- 公開URL: `https://sekisuihouse.github.io/tiune-shachiku-rpg/`
- GitHub Pages は `main` ブランチのルートを公開元としている。

## 主要操作

- `←` / `→`: 移動
- `↑`: ジャンプ
- `Z`: 第2ステージで空き缶を投げる。最終連打イベント中はエナドリ摂取ボタンの代替。
- `R`: リトライ（導入ストーリーは再表示しない）。

## 現在のゲーム設計

### 第1ステージ

- オフィス背景、穴・高低差・動く足場・ジャンプ台。
- 一般、魔法使い、投擲主の3職種の敵が計8人。
- 会議室（右端）に到達すれば敵を全滅させずに第2ステージへ進む。
- 中身入りエナドリが5本転がる。穴の手前で反転する。
- 開始時はスーパー勇者。被弾すると通常勇者に戻って一度耐える。
- 全5本飲むとカフェイン上限の死亡イベントになる。

### 第2ステージ

- 空き缶を最大9本ストックし、`Z` でラスボスへ投げてダメージを与える。
- ラスボス第1形態はゲロ散弾。
- HPをゼロにすると `phase2cut` 状態で時間停止し、第2形態カットインを表示する。
- 第2形態は散弾増加・レーザー・子分召喚を使用。子分の攻撃はエナドリ缶のみで、上から踏むと空き缶を1本得る。
- 第2形態撃破後は `finalStory` → `finalMash` → `bossDeath` の最終イベント。プレイヤーは無敵。
- 最終連打イベントではキャンバス上の「エナドリを摂取！」ボタンを30回クリック、または `Z` で連打できる。30回でカフェイン上限イベント。ボスが飲み始めてから60秒でボス死亡→クリア。

## アセット

- `BGM.mp3`: ループBGM
- `エナドリ.mp3`: エナドリの取得・使用時SE（macOS上では濁点が分解されたファイル名）
- `hero-normal.png`, `hero-super.png`: 主人公状態
- `general.png`, `wizard.png`, `thrower.png`: 敵職種
- `boss.png`, `boss-throw.png`, `boss-phase2.png`: ラスボス
- `caffeine-warning.png`: 最終連打30回時の呆れたラスボス警告画
- `awakening.png`: エナドリ取得時の覚醒カットイン
- `energy-grid.png`: 10種類のエナドリ。Canvasで切り出している。

## 実装上の注意

- ゲームの状態は `state` 文字列で管理している。状態を増やす場合は `update()` と `draw()` の両方に必ず分岐を追加する。
- キャラクターの足場判定は `solidMove()`、戦闘接触は `playerHitbox()`。見た目のサイズと戦闘判定を混同しない。
- `playEnergySound()` を呼ぶのは「エナドリの取得または使用」のすべての箇所。新しいエナドリ操作を追加したら必ず呼ぶ。
- GitHub Pages公開物なので、`index.html` から参照するアセットは必ずリポジトリにコミットする。
